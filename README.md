# Bikes Example

## Node-Redis Search and Aggregations Example

## Installation

Start a redis via docker:

``` bash
docker run -p 6379:6379 -it redis/redis-stack-server:latest
```

To install node-redis, simply:

```bash
npm install redis
```

## Running

```
npm start
```